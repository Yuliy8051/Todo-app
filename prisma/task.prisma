model Task {
    id Int @id @default(autoincrement())
    name String
    description String
    dueDate DateTime?
    status TaskStatus
    user User @relation(fields: [userId], references: [id])
    userId Int
}

enum TaskStatus {
    TO_DO
    IN_PROGRESS
    STOPPED
    DONE
}